// Native: no file picker dependency; backups are copied / pasted as text instead.
export const canUseFiles = false;
export const downloadTextFile = () => {};
export const pickTextFile = async () => null;
